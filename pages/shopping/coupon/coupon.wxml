<view class="container">

  <!-- <view class="coupon-list">

     <view class="item" wx:for="{{couponList}}" wx:key="{{item.id}}">
      <view class="tag" wx:if="{{item.send_type == 1 || item.send_type == 5 }}">普通礼品卡</view>
      <view class="tag" wx:if="{{item.send_type == 3}}">商品礼品卡</view>
      <view class="content">
        <view class="left">
          <view class="name">{{item.name}}</view>
          <view class="time">{{item.use_start_date}} - {{item.use_end_date}}</view>
        </view>
        <view class="right">
          <button class="go" bindtap='toUse' data-id='{{item.userCouponId}}' wx:if="{{item.isTransmit == 0 && item.isUsed == 0}}">使用</button>
        </view>
      </view>
      <view class="condition" wx:if="{{item.useDesc != null}}">
        <text class="txt">{{item.useDesc}}</text>
        <image src="https://www.resource.yuntuodev.cn/resource/images/weixin/couponArrow-71315b4145.png" class="icon"></image>
      </view>
       <view class="condition" wx:else>
        <text class="txt"></text>
        <image src="https://www.resource.yuntuodev.cn/resource/images/weixin/couponArrow-71315b4145.png" class="icon"></image>
      </view>
    </view>
  

  </view>
 -->

  <view class='Coupont'>我的优惠券</view>

  <view class='Couponlists'>

    <view class='Couponlists-cells'>
      <view class='Couponlists-cells-main'>
        <view class='Couponlists-cells-moneny'>
          ￥
          <text class='price'>5</text>
        </view>
        <view class='Couponlists-cells-descs'>
          <text class='proname'>果缤纷生鲜超市</text>
          <view>无门槛使用
              <text class='Couponlists-cells-coupontstatus'>即将过期</text>
        </view>
        </view>
       
      </view>
      <view class='Couponlists-cells-dataline'>
        限期使用：截止2018-10-01 24：00：00
      </view>
    </view>
  </view>



</view>